import { getFixtures } from '@/lib/data';
export default async function Fixtures(){const fixtures=await getFixtures();return <main className="shell"><nav className="nav"><a href="/">← GreyVerse</a><strong>⚽ Fixtures</strong></nav><section className="hero"><p className="muted">⚡ MATCH CENTER</p><h1>Fixtures</h1><p className="muted">Live competition schedule from GreyVerse.</p></section><div className="grid">{fixtures.map((f:any)=><article className="card" key={f.id}><div className="badge">⚽ {f.status??'Scheduled'}</div><h2>{f.home_player_name??f.home_team??'Home'} <span>vs</span> {f.away_player_name??f.away_team??'Away'}</h2><p className="muted">{f.scheduled_date?new Date(f.scheduled_date).toLocaleString():'Date TBA'}</p></article>)}{!fixtures.length&&<article className="card"><h2>⚽ No fixtures yet</h2><p className="muted">Fixtures will appear here when competitions have scheduled matches.</p></article>}</div></main>}
