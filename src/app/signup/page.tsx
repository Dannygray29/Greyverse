'use client';
import {FormEvent,useState} from 'react';
import {supabase} from '@/lib/supabase';
export default function Signup(){const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[msg,setMsg]=useState('');async function submit(e:FormEvent){e.preventDefault();if(!supabase){setMsg('Supabase is not configured.');return;}const{error}=await supabase.auth.signUp({email,password});setMsg(error?.message??'Account created. Check your email if confirmation is enabled.');}return <main className="shell"><a href="/">← GreyVerse</a><section className="hero"><h1>Create your GreyVerse account</h1><form onSubmit={submit} className="card"><input type="email" placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)} required/><input type="password" placeholder="Password (6+ characters)" minLength={6} value={password} onChange={e=>setPassword(e.target.value)} required/><button>Create account</button><p className="muted">{msg}</p></form></section></main>}