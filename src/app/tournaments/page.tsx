import {getTournaments} from '@/lib/data';
export default async function Tournaments(){const tournaments=await getTournaments();return <main className="shell"><nav className="nav"><a href="/">← GreyVerse</a><strong>🏆 Tournaments</strong></nav><section className="hero"><p className="muted">🏆 COMPETITIVE PLAY</p><h1>Tournaments</h1><p className="muted">Real GreyVerse competitions from Supabase.</p></section><div className="grid">{tournaments.map((t:any)=><article className="card" key={t.id}><div className="badge">🏆 {t.tournament_type}</div><h2>{t.tournament_name}</h2><p className="muted">Status: {String(t.status).trim()}</p><p className="muted">Maximum players: {t.max_players}</p><p className="muted">Entry: {t.entry_cost??0} points</p><p className="muted">{t.started_at?`Starts ${new Date(t.started_at).toLocaleString()}`:'Start date TBA'}</p><button disabled>Registration coming next</button></article>)}{!tournaments.length&&<article className="card"><h2>No tournaments found</h2><p className="muted">No tournaments were returned by the database.</p></article>}</div></main>}